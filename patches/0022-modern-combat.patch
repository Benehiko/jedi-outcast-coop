--- a/codeJK2/cgame/cg_local.h
+++ b/codeJK2/cgame/cg_local.h
@@ -591,6 +591,7 @@
 extern	vmCvar_t		cg_simpleItems;
 extern	vmCvar_t		cg_fov;
 extern	vmCvar_t		cg_fovAspectAdjust;
+extern	vmCvar_t		cg_fovSensitivityScale;
 extern	vmCvar_t		cg_fovViewmodel;
 extern	vmCvar_t		cg_fovViewmodelAdjust;
 extern	vmCvar_t		cg_missionstatusscreen;
--- a/codeJK2/cgame/cg_main.cpp
+++ b/codeJK2/cgame/cg_main.cpp
@@ -284,6 +284,7 @@
 vmCvar_t	cg_simpleItems;
 vmCvar_t	cg_fov;
 vmCvar_t	cg_fovAspectAdjust;
+vmCvar_t	cg_fovSensitivityScale;
 vmCvar_t	cg_missionstatusscreen;
 vmCvar_t	cg_endcredits;
 vmCvar_t	cg_updatedDataPadForcePower1;
@@ -350,6 +351,7 @@
 	{ &cg_drawGun, "cg_drawGun", "1", CVAR_ARCHIVE },
 	{ &cg_fov, "cg_fov", "80", CVAR_ARCHIVE },
 	{ &cg_fovAspectAdjust, "cg_fovAspectAdjust", "0", CVAR_ARCHIVE },
+	{ &cg_fovSensitivityScale, "cg_fovSensitivityScale", "0", CVAR_ARCHIVE },	// 0 = mouse sensitivity is FOV-independent (modern); 1 = legacy JK2 behavior (sensitivity scales with fov_y/75)
 	{ &cg_stereoSeparation, "cg_stereoSeparation", "0.4", CVAR_ARCHIVE  },
 	{ &cg_shadows, "cg_shadows", "1", CVAR_ARCHIVE  },
 
@@ -361,7 +363,7 @@
 	{ &cg_drawSnapshot, "cg_drawSnapshot", "0", CVAR_ARCHIVE  },
 	{ &cg_drawAmmoWarning, "cg_drawAmmoWarning", "1", CVAR_ARCHIVE  },
 	{ &cg_drawCrosshair, "cg_drawCrosshair", "1", CVAR_ARCHIVE },
-	{ &cg_dynamicCrosshair, "cg_dynamicCrosshair", "1", CVAR_ARCHIVE },
+	{ &cg_dynamicCrosshair, "cg_dynamicCrosshair", "0", CVAR_ARCHIVE },	// 0 = fixed screen-center crosshair (modern, no drift). 1 = legacy dynamic crosshair traced from the moving weapon muzzle along smoothed lerp-angles, which visibly lags/drifts behind the view.
 	{ &cg_crosshairIdentifyTarget, "cg_crosshairIdentifyTarget", "1", CVAR_ARCHIVE },
 	{ &cg_crosshairForceHint, "cg_crosshairForceHint", "1", CVAR_ARCHIVE|CVAR_SAVEGAME|CVAR_NORESTART },
 	{ &cg_missionstatusscreen, "cg_missionstatusscreen", "0", CVAR_ROM},
--- a/codeJK2/cgame/cg_view.cpp
+++ b/codeJK2/cgame/cg_view.cpp
@@ -1872,7 +1872,14 @@
 	const int		localForcePowers = haveLocalClient ? localGent->client->ps.forcePowersActive : cg.snap->ps.forcePowersActive;
 	const int		localEFlags = ( !cg_remoteClient && localGent ) ? localGent->s.eFlags : cg.snap->ps.eFlags;
 
-	float speed = cg.refdef.fov_y / 75.0 * ((localForcePowers&(1<<FP_SPEED))?1.0f:Q_min(cg_timescale.value, 1.0f));
+	// Mouse sensitivity used to be hard-scaled by (fov_y / 75): at the default
+	// sub-75 FOV this quietly divides your turn speed, which reads as "sluggish"
+	// aim on a modern high-DPI/high-refresh mouse, and it also made sensitivity
+	// swing every time the FOV changed. Decouple it by default (cg_fovSensitivityScale 0
+	// => 1:1 mouse, FOV-independent). Set cg_fovSensitivityScale to 1 to restore the
+	// legacy FOV-linked feel.
+	float fovSens = ( cg_fovSensitivityScale.value > 0.0f ) ? ( cg.refdef.fov_y / 75.0f ) : 1.0f;
+	float speed = fovSens * ((localForcePowers&(1<<FP_SPEED))?1.0f:Q_min(cg_timescale.value, 1.0f));
 
 //FIXME: junk code, BUG:168
 
--- a/codeJK2/game/g_active.cpp
+++ b/codeJK2/game/g_active.cpp
@@ -1994,6 +1994,7 @@
 	if ( ent->s.number == 0 )
 	{
 extern cvar_t	*g_skippingcin;
+extern cvar_t	*g_skipIntroCinematics;
 
 		if ( ent->s.eFlags & EF_LOCKED_TO_WEAPON )
 		{
@@ -2046,6 +2047,15 @@
 		{
 			// watch the code here, you MUST "return" within this IF(), *unless* you're stopping the cinematic skip.
 			//
+			// g_skipIntroCinematics: auto-fast-forward scripted map cutscenes without
+			// waiting for the player to press use. Kick off the same skip path the use
+			// button would; once skipping, fall through to the normal think so a second
+			// use press (or the cutscene ending) can still stop the skip.
+			if ( g_skipIntroCinematics->integer && !g_skippingcin->integer )
+			{
+				G_StartCinematicSkip();
+				return;
+			}
 			if ( ClientCinematicThink(ent->client) )
 			{
 				if (g_skippingcin->integer)	// already doing cinematic skip?
--- a/codeJK2/game/g_main.cpp
+++ b/codeJK2/game/g_main.cpp
@@ -160,6 +160,7 @@
 cvar_t	*g_ICARUSDebug;
 cvar_t	*com_buildScript;
 cvar_t	*g_skippingcin;
+cvar_t	*g_skipIntroCinematics;
 cvar_t	*g_AIsurrender;
 cvar_t	*g_numEntities;
 cvar_t	*g_iscensored;
@@ -561,6 +562,7 @@
 	gi.cvar( "gamename", JK_VERSION , CVAR_SERVERINFO | CVAR_ROM );
 	gi.cvar( "gamedate", SOURCE_DATE , CVAR_ROM );
 	g_skippingcin = gi.cvar ("skippingCinematic", "0", CVAR_ROM);
+	g_skipIntroCinematics = gi.cvar ("g_skipIntroCinematics", "0", CVAR_ARCHIVE);//when 1, scripted map cutscenes auto-fast-forward instead of waiting for the player to press use to skip
 
 	// latched vars
 
@@ -587,7 +589,7 @@
 	g_saberRealisticCombat = gi.cvar( "g_saberRealisticCombat", "0", CVAR_ARCHIVE );//makes collision more precise, increases damage
 	g_saberMoveSpeed = gi.cvar( "g_saberMoveSpeed", "1", CVAR_ARCHIVE|CVAR_CHEAT );//how fast you run while attacking with a saber
 	g_saberAnimSpeed = gi.cvar( "g_saberAnimSpeed", "1", CVAR_ARCHIVE|CVAR_CHEAT );//how fast saber animations run
-	g_saberAutoAim = gi.cvar( "g_saberAutoAim", "1", CVAR_ARCHIVE|CVAR_CHEAT );//auto-aims at enemies when not moving or when just running forward
+	g_saberAutoAim = gi.cvar( "g_saberAutoAim", "0", CVAR_ARCHIVE );//auto-aims (snaps facing/swings) at enemies when not moving or just running forward; off by default for modern free aim, set to 1 for legacy behavior
 
 	g_AIsurrender = gi.cvar( "g_AIsurrender", "0", CVAR_CHEAT );
 	g_numEntities = gi.cvar( "g_numEntities", "0", CVAR_CHEAT );
--- a/codeJK2/game/weapons.h
+++ b/codeJK2/game/weapons.h
@@ -169,7 +169,7 @@
 
 // Bryar Pistol
 //--------
-#define BRYAR_PISTOL_VEL			1800
+#define BRYAR_PISTOL_VEL			3600	// modern combat: ~2x for a snappy, near-hitscan feel (was 1800)
 #define BRYAR_PISTOL_DAMAGE			14
 #define BRYAR_CHARGE_UNIT			200.0f	// bryar charging gives us one more unit every 200ms--if you change this, you'll have to do the same in bg_pmove
 
@@ -178,7 +178,7 @@
 #define BLASTER_MAIN_SPREAD			0.5f
 #define BLASTER_ALT_SPREAD			1.5f
 #define BLASTER_NPC_SPREAD			0.5f
-#define BLASTER_VELOCITY			2300
+#define BLASTER_VELOCITY			4600	// modern combat: ~2x for a snappy, near-hitscan feel (was 2300). NPC shots are still cut by BLASTER_NPC_VEL_CUT so they stay evadable.
 #define BLASTER_NPC_VEL_CUT			0.5f
 #define BLASTER_NPC_HARD_VEL_CUT	0.7f
 #define BLASTER_DAMAGE				20
@@ -203,7 +203,7 @@
 // Wookie Bowcaster
 //----------
 #define	BOWCASTER_DAMAGE			45
-#define	BOWCASTER_VELOCITY			1300
+#define	BOWCASTER_VELOCITY			2600	// modern combat: ~2x (was 1300)
 #define	BOWCASTER_NPC_DAMAGE_EASY	12
 #define	BOWCASTER_NPC_DAMAGE_NORMAL	24
 #define	BOWCASTER_NPC_DAMAGE_HARD	36
@@ -220,7 +220,7 @@
 #define REPEATER_SPREAD				1.4f
 #define REPEATER_NPC_SPREAD			0.7f
 #define	REPEATER_DAMAGE				8
-#define	REPEATER_VELOCITY			1600
+#define	REPEATER_VELOCITY			3200	// modern combat: ~2x (was 1600)
 #define	REPEATER_NPC_DAMAGE_EASY	2
 #define	REPEATER_NPC_DAMAGE_NORMAL	4
 #define	REPEATER_NPC_DAMAGE_HARD	6
@@ -229,7 +229,7 @@
 #define	REPEATER_ALT_DAMAGE				60
 #define REPEATER_ALT_SPLASH_DAMAGE		60
 #define REPEATER_ALT_SPLASH_RADIUS		128
-#define	REPEATER_ALT_VELOCITY			1100
+#define	REPEATER_ALT_VELOCITY			2200	// modern combat: ~2x (was 1100)
 #define	REPEATER_ALT_NPC_DAMAGE_EASY	15
 #define	REPEATER_ALT_NPC_DAMAGE_NORMAL	30
 #define	REPEATER_ALT_NPC_DAMAGE_HARD	45
@@ -237,7 +237,7 @@
 // DEMP2
 //----------
 #define	DEMP2_DAMAGE				15
-#define	DEMP2_VELOCITY				1800
+#define	DEMP2_VELOCITY				3200	// modern combat: ~1.8x (was 1800)
 #define	DEMP2_NPC_DAMAGE_EASY		6
 #define	DEMP2_NPC_DAMAGE_NORMAL		12
 #define	DEMP2_NPC_DAMAGE_HARD		18
